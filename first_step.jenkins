#!groovy

properties([disableCuncurrentBuilds()])

piplene {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamp()
    }
    stages{
        stage("First step") {
            steps {
                echo " ======= start building image ======="
            }
        }
    }
}